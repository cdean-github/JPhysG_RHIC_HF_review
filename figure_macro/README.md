A template for making a figure is provided. The figure style is set in ```style.C```, and an example macro (```plots.C```) is provided for producing a figure. 

To test the code, use
```
root plots.C
```
